import { NodePath, Visitor } from '@babel/traverse';
import * as t from '@babel/types';
export default function normalizeBindings(path: NodePath, replacement: t.Expression, targetIdentifier: t.Identifier): Visitor;
