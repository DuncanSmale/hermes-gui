import { NodePath } from '@babel/traverse';
import * as t from '@babel/types';
import { ImportHook } from './types';
export default function getHookIdentifier(hooks: ImportHook, path: NodePath, name: string, source?: string): t.Identifier;
