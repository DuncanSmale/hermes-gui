import { createMemo } from "solid-js";
function useRef(initialVal = null) {
  return {
    current: initialVal
  };
}
function useCallback(fn, deps) {
  return fn;
}
function useMemo(fn) {
  return createMemo(fn)();
}
export { useCallback, useMemo, useRef };
